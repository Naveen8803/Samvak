services:
  - type: web
    name: samvak
    runtime: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      pip install gunicorn
      python -m spacy download en_core_web_sm
    startCommand: gunicorn --worker-class eventlet -w 1 --bind 0.0.0.0:$PORT app:app --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.7"
      - key: SECRET_KEY
        generateValue: true
