# Samvak — Python dependencies (Production / Render deploy)
# Install with: pip install -r requirements.txt

# ── Web framework ──────────────────────────────────────────────────────────
flask
flask-cors
flask-login
flask-sqlalchemy
flask-socketio
python-socketio
gunicorn                  # production WSGI server (Procfile)
eventlet                  # async driver for flask-socketio real-time streaming

# ── Auth / security ────────────────────────────────────────────────────────
werkzeug

# ── Core ML (lightweight) ─────────────────────────────────────────────────
numpy<2.0

# ── Computer vision ────────────────────────────────────────────────────────
opencv-python-headless    # headless for server (no GUI needed)
mediapipe                 # holistic landmark extraction (sign.py)
Pillow                    # image decode for base64 frame input

# ── Speech recognition ─────────────────────────────────────────────────────
SpeechRecognition         # Google STT wrapper (speech.py)
pydub                     # audio format conversion (mp3/ogg → wav)

# ── Text-to-speech ─────────────────────────────────────────────────────────
gTTS                      # Google TTS (text-to-speech fallback)

# ── Translation ────────────────────────────────────────────────────────
deep-translator           # Google Translate wrapper for output language

# ── NLP ────────────────────────────────────────────────────────────────────
spacy                     # lemmatisation for speech-to-sign tokenisation

# ── Hugging Face ───────────────────────────────────────────────────────────
huggingface_hub           # model download utilities

# ── Misc ───────────────────────────────────────────────────────────────────
flask-jwt-extended        # JWT tokens (used by auth.js API path)

# ── Heavy ML (optional — install locally for full backend inference) ───────
# tensorflow-cpu>=2.15    # LSTM sign model (browser TFJS handles this)
# torch                   # HF image classifier fallback
# torchvision             # image transforms for torch model
# scikit-learn            # evaluation utilities
# scipy                   # signal processing
